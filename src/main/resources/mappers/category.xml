<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.starbucksorder.another_back.repository.CategoryMapper">


    <select id="getCategory" resultType="com.starbucksorder.another_back.entity.Category">
        select
            category_id as categoryId,
            category_name as categoryName,
            category_status as categoryStatus,
            create_date as createDate,
            update_date as updateDate,
            category_seq as categorySeq
        from
            category_tb
        order by categorySeq
    </select>
</mapper>